export type DepositoCreateType = {
    nome: string;
    descricao: string;
    id_pessoa: string;
};

export type DepositoUpdateType = {
    nome: string;
    descricao: string;
    id_pessoa: string;
};
